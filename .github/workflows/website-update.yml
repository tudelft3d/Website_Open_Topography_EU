name: website update

on: 
  push:
    branches: 
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: trigger update
        run: curl --fail -L ${{ secrets.UPDATE_URL }}
